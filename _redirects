# Cloudflare Pages/static fallback routing.
# Keep product share routes app-owned. General /api/* traffic is handled by
# worker.js before static asset routing, so this file must not declare an
# absolute proxy redirect that Wrangler Assets rejects during deployment.
/api/product/* /product/:splat 302
